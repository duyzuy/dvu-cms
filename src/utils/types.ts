export enum PostStatus {
  PUBLISH = 'publish',
  APPROVED = 'approved',
  PENDING = 'pending',
  DRAFT = 'draft',
  UNPUBLISH = 'unpublish',
}

export enum PostTypes {
  POST = 'post',
  PAGE = 'page',
}

export enum CategoryStatus {
  ACTIVE = 'active',
  DEACTIVE = 'deactive',
}
